---
description: Vue3 接口请求与 JS 规范（联调契约、页面接数；编辑 src/api 时自动生效）
globs:
  - "src/api/**/*.js"
alwaysApply: false
---

# Vue3 接口请求与 JS 规范

> 适用场景：页面请求、入参/出参、JS 书写、删 helper / 内联重构。文中 `src/api/`、`doc/api` 等为**示例路径**，复制到其它项目时替换为该校验的 API 目录与接口文档位置。

> **本文件由 `cursor-skills` 同步生成，请勿手改；项目差异写在 `uni-app/project.local.mdc`。**

## 接口调用规范
- API 方法统一定义在 `src/api/` 下对应模块，页面层通过 import 调用，不在页面里硬编码 request。
- 页面请求返回数据后，优先直接赋值渲染（如 `list.value = res.data`），避免不必要的数据重组。
- 能顺序发起的请求就分开调用，不强制并发聚合。
- 接口异常处理依赖全局拦截器，页面层默认不重复写错误提示逻辑（除非用户明确要求）。
- 页面内请求数据时，直接使用 API 方法返回结果赋值：
  - 不做 `map` 重组后再赋值（直接 `list.value = res.data`）。
  - 不在页面层写 `catch` 做报错提示（错误由全局拦截器处理）。
- 子组件自身展示所需的数据，默认在子组件内部请求接口；父页面不做中转拉取后再透传（除非业务明确要求由页面统一编排请求）。
- `GET` 接口参数默认采用你项目里的固定写法：`http.get('/xxx', params)`；即 API 方法签名用 `const fn = (params) => {}`，参数通过第二个入参传入，不在页面层拼接 query 字符串。
- 创建接口方法时，参数签名统一写 `params`，不写默认值 `params = {}`。
- 仅当后端契约明确要求路径参数时，才写成 `/xxx/${id}`；否则优先保持 `http.get(url, params)` 风格一致。

## 联调 / 优化时禁止私自改契约（强制）

> 适用：用户说「接口联调」「按 api-js 优化」「加注释」「整理代码」等，且**未**写明可改参数或业务逻辑时。

**默认禁止（须用户明确授权才可改）：**

- 修改页面/API 调用处**已有**的 `params`、`data` 字段名或字段集合（含「对齐 `src/api` 注释」而改名，如 `homeSlideshowType` ↔ `slideshowType`）
- 新增或删除 query/body 字段（如擅自加 `current`、`size`、`pageSize` 等）
- 修改路由 `query` 参数名、跳转 URL、`onEnroll` / 提交等业务分支
- 新增前端条数限制（如 `slice(0, N)`、`HOME_XXX_LIMIT`）除非用户明确要求

**默认允许（无需额外授权）：**

- 注释、`base.mdc` 要求的 `<script setup>` 块顺序
- 拦截器已解包后的赋值路径修正（如 `page.data.records` → `page.records`），**不改**请求入参
- 用户任务里列出的文件范围内的联调接入

**发现不一致时：**

- 页面实现与 `src/api` 注释、或页面与 `doc/api` 文档不一致 → 在回复中说明，**不要**私自改页面入参去「对齐注释」
- 仅当用户写出「可以改参数」「按文档改参数」「允许改动字段：…」之一时，方可改对应字段

与 `base.mdc`「用户收窄改动范围」同时 @ 时，**取更严**。

## 输入输出要求
- 保持现有交互与视觉风格一致。
- 不引入无关依赖，不增加无关复杂度。
- 字段命名与后端契约保持一致，避免多余映射和过度兜底链式写法。
- 接口字段严格按契约使用：字段已确定时只用唯一字段名，不写猜测式多别名兜底（如 `a ?? b ?? c ?? d`）。

## 生成偏好：少无效层（通用）

> 可作提示词：**按当前接口/路由契约直接接数据；能写一行就不拆一层；没有明确要求就不加兼容分支与重复提示。**

- **以契约为准**：入参、出参、路由 query 用什么字段，就写什么字段；不为「可能以后会变」预写多套别名、多层解码、大包小包的 `normalize*`。
- **不为抽象而抽象**：若只是类型或一两次赋值，在调用处处理即可；默认不单独抽「校验通过才继续」的中间层，除非多处复用或用户点名要。
- **删 helper / 内联化时（强制）**：删除、重命名或内联替换封装函数（如 `withXxx`、`buildXxxParams`）前，必须先**全项目搜索**该符号名，改齐**全部**调用点后再删定义；禁止只改一处调用就删除函数体（易导致运行时 `ReferenceError`、接口不发请求）。
- **与全局能力分工**：已有全局拦截器（或其它统一错误处理）时，页面里不重复做同类失败提示；页面 `catch` 以收尾（状态、loading）为主。
- **和具体页面对齐**：分支、校验、空态是否要写，以该页业务与用户说明为准；本节只约束「无依据的堆判断」，不替代表单校验、权限、埋点等真实需求。
- **用户明确不要判断时（强制）**：若用户写明「不需要加判断条件 / 不要判空校验 / 不要防御分支」，页面层不得新增参数缺失、类型校验（如 `Number.isNaN`）或兜底跳转分支；按现有接口契约直连调用。

## 重构与契约一致性（强制，跨项目通用）

> 删函数、内联参数、优化「少无效层」时必读本节，避免半改漏改。与具体业务域无关，适用于任意前后端分离项目。

### 删除 / 重命名 / 内联替换

1. **改前**：在当前仓库内搜索待删/待改符号（函数名、变量名、路由参数名、导出名），列出**全部**引用文件与行。
2. **改中**：同一符号的**每一处**引用同步更新，不得遗漏（含次要分支：弹窗确认、定时器回调、重试、埋点/上报、卸载钩子中的调用等）。
3. **改后**：再次全仓库搜索，确认**零残留**（旧符号名无引用，或仅在注释中保留且标明已废弃）。
4. **回复中须说明**：改动文件列表、是否已完成全量引用检查（建议写「已全仓搜索 `符号名`，无残留」）。

### 同一业务链路的契约字段

当用户或接口文档要求某字段贯穿**同一条业务链路**（多页面、多步骤、或单页多接口）时：

1. **入口与传递**：在流程入口（列表项、路由跳转、状态初始化）取得该字段，并在后续页面/步骤间**原样传递**（路由 query、路由 params、Pinia/Vuex、或父组件 props，以项目既有方式为准）；字段名与后端契约**唯一**，不写多别名兜底。
2. **接口全覆盖**：列出该流程内所有相关请求（如 list / get / create / update / submit / upload 等），逐项核对 body、query、path 是否均需该字段；**禁止只改部分接口而遗漏其它调用点**（含同一文件内不同事件触发的请求）。
3. **删包装函数时**：为减少层级而内联参数时，须在**每一个**请求调用处显式传入该字段，并执行上一小节的「删前搜、改后搜」。

### 提交前自检（接口相关改动）

- [ ] 已删除的 helper / 旧符号在全仓库无引用
- [ ] 本需求涉及的请求清单已逐项核对参数字段
- [ ] 路由/状态传递与请求参数字段名与**项目接口文档**或用户说明一致
- [ ] 本次若未获用户授权改参数，则**未**改动任何既有请求入参、query 名、业务跳转逻辑

## JS 规范（强制，ES6+）
1. 方法之间必须保持一行空行分隔。
2. 变量声明后空一行再写逻辑代码。
3. 自定义业务函数统一使用箭头函数：`const fn = () => {}` 或 `const fn = async () => {}`，不使用 `function fn() {}` 声明。
4. 异步接口调用优先 `async/await` 顺序书写，避免多层 `.then` 嵌套；需要并行时用 `Promise.all` / `Promise.allSettled` 等标准 API。
5. **不要求**在生命周期、事件回调里对「不 await 的 async 调用」写 `void` 前缀（如 `void load()`）；直接写 `load()` 即可；是否用 `void` 以满足 ESLint floating-promise 等规则由项目自身配置决定，本规范不强制。
6. 绑定使用 `const` / `let`，禁止 `var`。
7. 动态字符串使用模板字面量（如 `` `/path/${id}` ``），与上文路径参数约定一致。
8. 在可读性提升时使用解构赋值、剩余/展开运算符；可选链 `?.`、空值合并 `??` 仅用于契约明确的字段，不与「不写猜测式多别名兜底」冲突。

## 与 `base.mdc` 的分工（@ 本文件时必读）
- 本节只约束 **接口封装、请求方式、页面接数据与 JS 书写**。
- 用户若写明 **「不要加代码 / 只动注释 / 当前实现最合理」** 等收窄范围，或对 **`@vue3/base.mdc`** 有同样说明：即使 @ 了本文件，也 **不得** 借机改业务分支、路由 query、或新增「更稳妥」的接口与判空；**改动范围以 `base.mdc` 中「用户收窄改动范围」为准**，两文件同时 @ 时 **取更严**。
