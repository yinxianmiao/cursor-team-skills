---
description: Vue3 公用风格（脚本/命名/样式/模板/可维护性/AI 协作）
globs: "**/*.{vue,js,ts}"
alwaysApply: false
---

# Vue3 公用风格与约定

> 与本仓库「工程层」约定（技术栈、@/api、目录等）见 `vue3/base.mdc`。

> **本文件由 `cursor-skills` 同步生成，请勿手改；项目差异写在 `uni-app/project.local.mdc`。**

## 脚本风格（`<script setup>`）
- 自定义业务函数统一使用箭头函数：`const fn = () => {}` 或 `const fn = async () => {}`，不使用 `function fn() {}`。
- 默认不使用 TypeScript：页面与组件脚本统一使用 `<script setup>`，不写 `lang="ts"`。
- 函数之间空一行：包括 `const xxx = () => {}`、`computed`、`watch`、生命周期钩子等。
- 推荐自上而下顺序（新增代码尽量归入对应区块）：
  1. `import`（项目已配置 auto-import，Vue API 可不手写 import）
  2. `defineProps` / `defineEmits` / `defineExpose`（如有）
  3. `ref` / `reactive` / 常量配置
  4. `computed`
  5. `watch` / `watchEffect`（如有）
  6. 生命周期（`onMounted` 等）
  7. 自定义业务函数
- 分区注释建议按上述顺序生成，推荐使用：
  - `// ---------------- 基础引入 ----------------`
  - `// ---------------- 组件通信定义 ----------------`
  - `// ---------------- 数据模型 ----------------`
  - `// ---------------- 计算属性 ----------------`
  - `// ---------------- 监听/副作用 ----------------`
  - `// ---------------- 生命周期 ----------------`
  - `// ---------------- 业务方法 ----------------`
- 不同板块（变量区、computed、生命周期、函数区）空一行以上区分。
- 模板里避免复杂表达式，复杂逻辑放到 `computed` 或函数中。
- 接口异步调用优先 `async/await` 顺序书写，避免多层 `.then` 嵌套；需要并行时用 `Promise.all` / `Promise.allSettled`；与 `api-js.mdc` 中页面层不写冗余 `catch` 等约定一致。
- **脚本结构顺序（强制）**：`<script setup>` 内代码顺序必须为：
  1. `import`
  2. `defineProps` / `defineEmits` / `defineExpose`
  3. 数据模型（`ref` / `reactive` / 常量）
  4. `computed`
  5. `watch` / `watchEffect`
  6. 生命周期（`onLoad` / `onShow` / `onMounted` 等）
  7. 业务方法
- 禁止将生命周期写在业务方法之后；如需重构，先调整顺序再提交。

## 命名与状态
- 业务页面内引入的子组件可用 PascalCase（如 `partitionManagement1`）。
- 跨页面复用的通用组件目录/命名建议使用 `com-` 前缀（如 `com-table`、`com-tree`）。
- 布尔状态命名清晰：`loading`、`visible`、`isXxx`，避免 `flag1`、`status2`。
- 与接口强相关字段优先保持后端字段名一致，减少无谓映射。
- 多条件决定的 UI 行为优先用 `computed` 表达。

## 组件通信（`<script setup>`）
- 使用 `defineProps` / `defineEmits` 定义 props 与事件；需要暴露给父组件调用时使用 `defineExpose`。

## 模板约定
- 列表加 `:key`，优先用业务 id，避免用 index 作为唯一 key。
- `template` 注释聚焦“结构分区”和“业务意图”，避免对单行标签做无意义解释。
- 页面/组件的 `template` 推荐使用分区注释：如 `<!-- 顶部信息区 -->`、`<!-- 列表区 -->`、`<!-- 底部操作区 -->`。
- 复杂条件渲染（`v-if / v-else-if / v-else`）前必须补 1 行注释，说明分支触发条件与业务原因。
- `v-for` 渲染块前建议补注释说明数据来源与交互目的（如“活动卡片列表，点击进入详情”）。
- 注释内容优先写“为什么这样拆分/展示”，不重复标签字面含义（如避免“这是一个 div”）。
- 注释数量保持克制：同一层级连续结构可用 1 条总注释，不要为每个子节点都写注释。

## 样式约定
- 样式统一 `scss` + `scoped`，避免全局污染。
- 页面根节点使用语义化 class（如 `app-container`、`pageBox`），避免裸 div 堆叠。
- 常见布局优先 `flex`，与项目现有页面风格保持一致。
- **SCSS 嵌套写法（强约束）**：必须使用父级包裹子级的嵌套写法；除全局工具类外，禁止同层大段平铺选择器。
- 提交前必须自检：页面/组件样式是否按 DOM 层级嵌套，避免可嵌套却扁平展开的写法。例如：
  ```scss
  .parent {
    .child {
      // 子级样式
    }
    .another-child {
      // 另一子级样式
    }
  }
  ```

# ====================== 代码风格 ======================
1. 缩进使用 2 空格
2. 方法按：数据模型 → 生命周期 → 搜索/重置 → 表格操作 → 弹窗操作 排序
3. 注释简洁清晰，关键逻辑必须说明
4. **每个函数结束后必须空一行**
5. 代码保持整洁，避免无意义空行与冗余注释

# ====================== JS 格式规范（强制） ======================
1. 方法之间必须保持一行空行分隔
2. 变量声明后空一行再写逻辑代码；绑定使用 `const` / `let`，禁止 `var`
3. 异步逻辑优先 `async/await`，避免冗长 `.then` 链
4. 方法顺序上，获取详情/初始化数据的方法（如 `getDetail`、`fetchDetail`）必须放在业务方法区靠前位置，优先于交互处理方法（如点击、提交、弹窗开关等）
5. 涉及 `<script setup>` 的改动，提交前必须自检并确认生命周期位于业务方法之前
6. 涉及 `<script setup>` 的改动，回复中必须显式说明“结构顺序已对齐 base.mdc”；未对齐不得结束本次修改

# ====================== 提交前自检（强制） ======================
1. 每次修改 `.vue` 文件后，必须逐项自检：
   - 生命周期是否位于业务方法之前
   - 函数之间是否保持一行空行分隔
   - 变量声明后是否空一行再写逻辑
2. **涉及删除/重命名函数、内联 helper、或同一契约字段贯穿多页面/多接口时**：另按 `vue3/api-js.mdc` 中「重构与契约一致性」执行删前搜、改后搜、请求清单核对（规则跨项目通用，复制 mdc 即可复用）。
3. 任一项不满足，先修正再提交结果

# ====================== 组件封装规范（新增，强制） ======================
1. 封装通用组件**默认**采用“无样式组件（styleless）”，优先由页面侧控制视觉样式
2. 通用组件优先保留结构、props、events、slots 逻辑，减少与业务页面样式耦合
3. 若确实需要组件内置样式，允许在组件内写 `<style scoped>`，并在组件注释中简要说明原因

# 可维护性与可读性规范
maintainability:
  - 注释只写复杂条件、业务关键路径、易踩坑点，避免解释显而易见代码。
  - 单个方法过长或职责混杂时，优先拆分小函数。
  - 修改核心业务逻辑时，补充简短说明性注释，方便后续维护。
  - **SFC 文件顶部 `<!-- ... -->`（固定两条，勿增第三条）**：业务页面/组件统一用下面**两句各一行**（两条独立 `<!-- ... -->`），占位按实际替换：
    - 第 1 条：`<!-- 页面名：一句业务说明（主接口方法名括号写出，如 orderAppMyPage），主要去向或交互。 -->`
    - 第 2 条：`<!-- 接口封装见 @/api/模块目录；契约见 doc/api/对应文档.md；列表项字段与后端返回一致。 -->`（非列表页将「列表项字段…」改为「字段与后端返回一致。」即可。）
  - 顶部块注释主要用于全局/通用组件（如 `src/components` 下可复用组件）；业务页面内局部组件默认不强制。
  - 组件/页面文件若需补充元信息，优先挤在上述两条内；避免在文件头展开 `@author` / `@description` 长列表。
  - `/** ... */`（js/ts）单文件头部：**同样建议最多两行**简要模块说明，不再堆叠。
  - 每个函数都必须有注释；注释优先写“为什么”，不重复“做了什么”；复杂分支/判定条件前可补 1 行中文注释说明业务意图。
  - 对外暴露的 `props`、`emit`、公共方法或关键常量，建议补充简短注释说明含义、取值范围或副作用。
  - 注释与代码必须同步维护：重构、改名、删除逻辑时，同步更新或移除过期注释，禁止保留误导性注释。

## 与 AI 协作（Cursor）
- 生成新页/组件前先说明：业务场景、结构、是否可复用现有 `com-*` 或同目录组件。
- 复杂分支（支付、权限等）可先让 AI 用中文梳理「状态与分支」，再写代码。
- 生成代码时对齐本仓库现有页面：命名、布局、提示文案。
- 生成代码时避免“过度防御性写法”：不要无意义堆叠 `?.`、`??`、`|| []`、`|| {}`。仅在接口边界、异步返回不确定、或明确可能为空的场景使用必要判空。
- 接口字段严格按契约使用：字段已确定时只用唯一字段名，不写猜测式多别名兜底（如 `a ?? b ?? c ?? d`）。
- 若确需兼容旧字段，必须有依据（迁移期、后端确认、线上观测其一），并写简短注释说明来源与移除条件。

### 用户收窄改动范围（@ 本文件时强制）
- 当用户在同一条任务里 **@ 本文件**（`vue3/base.mdc`），并写明或同义表达以下任一类时：**除非用户随后明确写「可以改逻辑 / 可以重构 / 可以改行为」**，Agent **不得**擅自：增删业务分支、改路由或 query 名与拼法、增删接口与请求编排、改状态模型与交互结果、以「更合理 / 更规范 / 防踩坑」为由替换用户现有实现。
- **典型触发语**（含同义）：`不要增加代码`、`不要加代码`、`当前/我的实现最合理`、`只规范注释`、`仅改注释/格式`、`逻辑一行别动`、`只动样式不动 script`、`diff 不超过 N 行` 等。
- **补充触发语**（含同义）：`不需要加判断条件`、`不要加判空/参数校验`、`不要防御性分支`。命中后不得新增 `if` 校验分支、兜底 toast、兜底跳转或同类「保护性逻辑」。
- **此情况下允许**：注释与文档、换行与 import 顺序、`base.mdc` 已要求的分区注释与 `<script setup>` 块顺序（若用户未禁止调整顺序）、**仅因语法无法编译**的极小修复（缺括号/引号/非法字符）；若做语法级修复，回复中须**单独点明**「仅此一处、非业务改动」。
- 若其它规范条目与上述收窄**冲突**（例如某条要求必须新增分支），**不得在用户不知情下扩写实现**：须在回复中列出冲突，并默认**遵守用户收窄**直至用户明确放行。
- **与 `api-js.mdc` 的分工**：接口怎么接、页面是否写 `catch`、删 helper / 全量改引用 / 多接口同字段核对等以 `@vue3/api-js.mdc` 为准；**「能不能动业务逻辑」** 以本条为准；用户可同时 @ 两文件，二者**取更严**的范围。
