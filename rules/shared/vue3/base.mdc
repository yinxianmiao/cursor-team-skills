---
description: Vue3 公用风格（脚本/命名/模板/注释/可维护性/AI 协作；样式见 uni-app/style.mdc）
globs: "**/*.{vue,js,ts}"
alwaysApply: false
---

# Vue3 公用风格与约定

> 样式块、SCSS 嵌套、布局单位 → **`uni-app/style.mdc`**（唯一细则）。接口契约与联调禁令 → **`vue3/api-js.mdc`**。
>
> **本文件由 `cursor-skills` 同步生成，请勿手改；项目差异写在 `uni-app/project.local.mdc`。**

## 脚本风格（`<script setup>`）

- 自定义业务函数统一箭头函数：`const fn = () => {}` / `const fn = async () => {}`，不用 `function fn() {}`。
- 默认不用 TypeScript：统一 `<script setup>`，不写 `lang="ts"`。
- 函数之间空一行；变量声明后空一行再写逻辑；绑定用 `const` / `let`，禁止 `var`。
- 缩进 2 空格；模板里避免复杂表达式，复杂逻辑放 `computed` 或函数。
- 异步优先 `async/await`；并行用 `Promise.all` / `Promise.allSettled`；页面层冗余 `catch` 等见 `api-js.mdc`。

**`<script setup>` 结构顺序（强制）**——禁止生命周期写在业务方法之后：

1. `import`（已配置 auto-import 的 Vue API 可不手写）
2. `defineProps` / `defineEmits` / `defineExpose`（如有）
3. 数据模型（`ref` / `reactive` / 常量）
4. `computed`
5. `watch` / `watchEffect`（如有）
6. 生命周期（`onLoad` / `onShow` / `onMounted` 等）
7. 业务方法（`getDetail` / `fetch*` 等初始化靠前，再写点击/提交/弹窗等交互）

分区注释建议：

- `// ---------------- 基础引入 ----------------`
- `// ---------------- 组件通信定义 ----------------`
- `// ---------------- 数据模型 ----------------`
- `// ---------------- 计算属性 ----------------`
- `// ---------------- 监听/副作用 ----------------`
- `// ---------------- 生命周期 ----------------`
- `// ---------------- 业务方法 ----------------`

涉及 `<script setup>` 的改动：提交前自检顺序与空行；回复须显式说明「结构顺序已对齐 base.mdc」。

## 命名与状态

- 页内子组件可用 PascalCase；跨页通用组件建议 `com-` 前缀（如 `com-table`）。
- 布尔命名：`loading`、`visible`、`isXxx`，避免 `flag1`、`status2`。
- 与接口强相关字段优先与后端一致；多条件 UI 行为优先 `computed`。

## 组件通信与封装

- 用 `defineProps` / `defineEmits`；需父调子时用 `defineExpose`。
- 通用组件**默认 styleless**（结构 + props/events/slots，样式由页面控）；确需内置样式时写 `<style scoped>` 并在注释说明原因。

## 模板约定

- 列表 `:key` 优先业务 id，避免仅用 index。
- 分区注释写结构/意图（如 `<!-- 列表区 -->`）；复杂 `v-if` 分支前补 1 行说明触发条件；`v-for` 块前可补数据来源与交互目的。
- 注释写「为什么」，不重复标签字面含义；同一层级连续结构用 1 条总注释即可。

## 样式（指向）

- 样式细则（`scoped` + `scss`、嵌套、rpx、属性顺序等）**只维护在 `uni-app/style.mdc`**，本文件不重复展开。

## 注释分级（强制，消冲突）

| 级别 | 要求 |
|------|------|
| **必须** | 业务页/组件文件头固定**两条** `<!-- ... -->`（勿增第三条）；复杂/非直觉分支；对外 `props`/`emit`/公共方法或关键常量 |
| **建议** | 核心业务改动处补一句 why；js/ts 文件头最多两行模块说明 |
| **默认不写** | 一眼能懂的赋值、简单 getter/`computed`、纯展示逻辑 |

文件头两条（占位按实际替换）：

1. `<!-- 页面名：一句业务说明（主接口方法名括号写出），主要去向或交互。 -->`
2. `<!-- 接口封装见 @/api/模块目录；契约见 doc/api/对应文档.md；列表项字段与后端返回一致。 -->`（非列表页改为「字段与后端返回一致。」）

- 顶部块注释主要针对全局/通用组件；页内局部组件默认不强制文件头。
- 元信息挤进上述两条，禁止展开 `@author` / `@description` 长列表。
- 注释与代码同步维护；禁止保留误导性过期注释。
- 单个方法过长或职责混杂时优先拆小函数。

## 提交前自检（强制）

修改 `.vue` 后逐项确认：

1. 生命周期在业务方法之前；函数间空一行；变量声明后空一行再写逻辑
2. 删/重命名函数、内联 helper、或契约字段贯穿多页/多接口时：按 `vue3/api-js.mdc`「重构与契约一致性」删前搜、改后搜、请求清单核对
3. 任一项不满足则先修正再结束

## 与 AI 协作（Cursor）

- 生成新页/组件前先说明：业务场景、结构、是否可复用现有 `com-*` 或同目录组件。
- 复杂分支可先中文梳理「状态与分支」再写代码；对齐本仓现有命名、布局、文案。
- 避免过度防御：不无意义堆叠 `?.`、`??`、`|| []`、`|| {}`；仅在接口边界或明确可能为空处判空。
- 字段按契约唯一命名，不写 `a ?? b ?? c` 式多别名兜底；兼容旧字段须有依据并注明移除条件。

### 用户收窄改动范围（@ 本文件时强制）

用户 **@ 本文件** 且写出下列任一类（含同义）时：除非随后明确「可以改逻辑 / 可以重构 / 可以改行为」，**不得**擅自增删业务分支、改路由/query、增删接口编排、改状态模型与交互结果，或以「更合理/更规范」替换现有实现。

**典型触发语**：`不要增加代码`、`不要加代码`、`当前/我的实现最合理`、`只规范注释`、`仅改注释/格式`、`逻辑一行别动`、`只动样式不动 script`、`diff 不超过 N 行`。

**补充触发语**：`不需要加判断条件`、`不要加判空/参数校验`、`不要防御性分支` → 不得新增校验 `if`、兜底 toast/跳转等保护性逻辑。

**此情况下允许**：注释与文档、换行与 import 顺序、本文件要求的分区注释与 `<script setup>` 块顺序（用户未禁止时）、**仅因无法编译**的极小语法修复（须单独点明「仅此一处、非业务改动」）。

与其它规范冲突时：回复中列出冲突，**默认遵守用户收窄**直至明确放行。与 `@vue3/api-js.mdc` 同时 @ 时**取更严**（接口怎么接以 api-js 为准；能不能动业务逻辑以本条为准）。
