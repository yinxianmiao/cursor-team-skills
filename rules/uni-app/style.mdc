---
description: uni-app 样式与布局约定（跨项目通用，路径示例按各仓库约定替换）
globs:
  - "src/**/*.vue"
  - "src/**/*.scss"
  - "src/**/*.css"
alwaysApply: false
---

# uni-app 样式与布局约定

> 适用场景：页面/组件样式块、SCSS 嵌套、布局与视觉对齐。文中 `src/static/`、全局样式入口等为**示例路径**，复制到其它项目时替换为该校验的实际目录。

> **本文件由 `cursor-skills` 同步生成，请勿手改；项目差异写在 `uni-app/project.local.mdc`。**

## 样式块基础

- 默认写法：`<style scoped lang="scss">`。
- 普通页面/组件避免无 `scoped` 的样式；全局样式仅在约定入口处维护，并添加用途注释。
- 需要穿透子组件样式时使用 `:deep()`，仍放在 `scoped` 样式块内。
- 生成或修改 `.vue` 时，样式区块默认输出 `<style scoped lang="scss">`。

## 布局与视觉

- 本项目以 uni-app 多端为目标，布局默认优先移动端体验，单位优先 `rpx`，并兼顾不同端的展示一致性。
- 新建页面时**不要默认添加** `min-height: 100vh` 与页面根容器 `background`；仅在设计稿明确要求整屏铺底、背景延展或产品明确指定时再添加。
- 常见布局优先 `flex`，视觉与间距尽量与现有页面保持一致。
- 页面根节点使用语义化 class，避免裸 div 堆叠。
- 图标与图片统一从项目静态资源目录引用，路径命名保持语义化。

## 属性书写顺序

- CSS 属性统一按「布局定位 → 盒模型 → 视觉表现 → 文本排版 → 交互动画」顺序书写。
- 提交前排查重复或冲突属性。

## SCSS 嵌套（强约束）

- 必须使用父级包裹子级的嵌套写法；除全局工具类外，禁止同层大段平铺选择器。
- 提交前必须自检：页面/组件样式是否按 DOM 层级嵌套，避免可嵌套却扁平展开的写法。例如：

```scss
.parent {
  .child {
    // 子级样式
  }

  .another-child {
    // 另一子级样式
  }
}
```

## 与相关规则的分工

- **`vue3/base.mdc`**：模板分区注释、组件命名、脚本结构等与样式无直接冲突的 Vue 公用约定。
- **`uni-app/project.mdc`**：路由、TabBar、请求层、Pinia 等工程结构约定。
- **本文件**：样式块写法、SCSS 嵌套、布局单位与视觉对齐。
